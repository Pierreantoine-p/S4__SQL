# TRombin-o-clock

## Au menu du jour

-Organiser un projet
-SQL

## Commandes GIT

```TEst
git init # initialise un nouveau repo git en local : il crée le dossier .git

# ATTENTION là il faut un repo Github existant 
git remote add origin git@github.com <...> git # on lie le repo GH en local

git remote -v # pour voir le(s) remote(s) actuel(s)
git status # Montre les différence entre l'arbre de travail
et l'index GIT

git add README.md __ docs/CLIENT.md
git add . # Faire en sorte que le fichier (ou tousles fichiers avec `.`) soit suivi par git